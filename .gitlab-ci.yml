variables:
  GIT_STRATEGY: clone

stages:
  - build
  - test

include:
  - local: '/gitlab-ci/build/android/.gitlab-ci.yml'
  - local: '/gitlab-ci/build/gradle/.gitlab-ci.yml'
  - local: '/gitlab-ci/build/apk/.gitlab-ci.yml'

test:
  stage: test
  script:
    - echo "Testing"
    - test -f "build/info.txt"
