stages:
    - build
    - test

build:
    stage: build
    script:
        - echo "bulding"
        - mkdir build
        - touch build/info.txt
        
test:
    stage: test
    script:
        - echo "testing"
        - test f "build/info.txt"