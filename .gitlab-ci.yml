variables:
  GIT_STRATEGY: clone
  
stages:
  - gradle
  - android
  - build
  - test

include:
  - local:
#     '/build/apk/.gitlab-ci.yml'
     '/build/gradle/.gitlab-ci.yml '
test:
  stage: test
  script:
    - echo "Testing"
    - test -f "build/info.txt"
